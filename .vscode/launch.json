{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Run and debug executable 'modsync'",
      "program": "${workspaceFolder}/target/debug/modsync.exe",
      "preLaunchTask": "build: debug with config",
      "args": [],
      "cwd": "${workspaceFolder}",
      "terminal": "integrated"
    }
  ]
}